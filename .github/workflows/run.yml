name: collector

on:
workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'
 jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: start
        run: |
          echo "starting collector"

      - name: simulate work
        run: |
          curl -s https://api.coindesk.com/v1/bpi/currentprice.json > /dev/null

      - name: done
        run: |
          echo "cycle complete"
